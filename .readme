Vehicle Fleet Management CLI
This is a command-line interface (CLI) application for managing a vehicle fleet and tracking the location of each vehicle. It exposes the functionality of the fleet management system to the world.

Installation
To install the application, make sure you have Node.js and npm (Node Package Manager) installed on your machine. Then, follow these steps:

Clone the repository: git clone <repository-url>
Navigate to the project directory: cd vehicle-fleet-management-cli
Install the dependencies: npm install
Usage
The CLI provides the following commands:

Create a Fleet
To create a fleet, use the following command:

bash
Copy code
./fleet create <userId>
Replace <userId> with the unique identifier of the user. This command returns the fleet ID on the standard output.

Register a Vehicle
To register a vehicle to a fleet, use the following command:

js
Copy code
./fleet register-vehicle <fleetId> <vehiclePlateNumber>
Replace <fleetId> with the ID of the fleet and <vehiclePlateNumber> with the plate number of the vehicle.

Localize a Vehicle
To track the location of a vehicle in a fleet, use the following command:

js
Copy code
./fleet localize-vehicle <fleetId> <vehiclePlateNumber> <latitude> <longitude> [altitude]
Replace <fleetId> with the ID of the fleet, <vehiclePlateNumber> with the plate number of the vehicle, <latitude> with the latitude of the vehicle's location, <longitude> with the longitude of the vehicle's location, and [altitude] (optional) with the altitude of the vehicle's location.

Scripts
The following scripts are available for development and testing:

npm test: Runs the tests using Cucumber with production tags.
npm run test-dev: Runs the tests using Cucumber without any tags.
npm run build: Transpiles the TypeScript code to JavaScript.
npm run exe: Executes the transpiled code.
npm run vehicle: Runs the command to create a vehicle.
npm run fleet: Runs the command to create a fleet.
npm run fleet-add: Runs the command to register a vehicle to a fleet.
npm run localize-vehicle: Runs the command to localize a vehicle in a fleet.
Dependencies
This project uses the following dependencies:

commander: For command-line option parsing.
dotenv: For loading environment variables from a .env file.
mongodb: The official MongoDB driver for Node.js.
License
This project is licensed under the proprietary license.